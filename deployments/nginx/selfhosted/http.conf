# сертификата ещё нет: сайт по http и acme-challenge для certbot
server {
    listen 80;
    server_name ${DOMAIN};

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    include /etc/nginx/tjudge/locations.conf;
}
